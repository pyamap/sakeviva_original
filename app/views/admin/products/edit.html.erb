<%= stylesheet_link_tag "desktop/web/products", :media => "all" %>

<div id="product_registration">
	<%= form_for [:admin, @product] do |form| %>
	<%= render "form", form: form %>
	<%= form.submit %>
	<% end %>
	<p><%= link_to "商品の詳細に戻る", [:admin, @product] %></p>
</div>

